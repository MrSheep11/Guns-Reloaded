# Runs as and at the shooter. This is an ordinary positional playsound, so
# listeners hear it from the gun's world position rather than as global audio.
$playsound $(fire_sound) player @a[distance=..32] ~ ~ ~ 1 1
